package main;

/**
 * 判断是否为数字
 * 有限状态机
 * @author Song Rui
 */
public class IsNumberOffer20 {

    public boolean isNumber(String s) {
        if(s.length() == 0){
            return false;
        }
        char[] chars = s.toCharArray();
        int len = s.length();
        return false;
    }
}
