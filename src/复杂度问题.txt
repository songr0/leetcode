当对时间和空间复杂度有要求时，

时间上，当有一个一维或者

空间上，可以借助一切能够利用的空间，如238 除自身以外数组的乘积，返回数组在计算完成前作为暂存数组
最终结果在暂存的基础上进行计算；
利用动态数组。
分左右两边计算后，计算结果为output[i] = l[i] * r[i]，
output结果可以从中间数据中直接产出，故可以和l或r共用，
又r[i]的值可以从r[i+1]推出，且r[i+1]只使用一次，故一个变量即可存储
